# CLAUDE.md - ten-bagger 작업 규칙

## 지속 갱신 문서는 2개뿐 (SoT = main)
- **`STYLE_GUIDE.md`** — 디자인 SoT(토큰·해도 레이어·컴포넌트). **신규 메뉴는 「01 시장 모니터링 = 레퍼런스 구현」을 복제한다.**
- **`OPS.md`** — 운영 SoT(불변 규율·정보 인벤토리·갱신 주기·GitHub 파이프라인·자기갱신 매핑표).
- **작업 시작 전 이 둘을 `main`에서 재페치해 읽는다.** Claude Project 지식파일 캐시는 폴백 — 충돌하면 리포가 이긴다.
- **작업 후 같은 PR에서 해당 문서를 갱신**한다(무엇→어디는 `OPS.md` §7 매핑표). 하단 갱신 이력에 한 줄.
- `INFO_SOURCES.md`는 `OPS.md` §3으로 흡수·삭제(2026-07-12).

## 브랜치/배포
- 항상 main 에서 분기한 새 브랜치에서 작업하고, PR base 는 반드시 main 로 한다.
- main 에 직접 push 금지. 변경은 PR 로만.
- **GitHub 코드 수정 요청은 별도 승인 요청 없이 PR 생성·검증·병합 확인까지 완료한다.** 사용자가 명시적으로 중단·검토만 요청한 경우에만 병합 전 멈춘다.

## 개인 투자 분석 원칙
- 모든 답변은 한국어로 작성하며, 종목명·제품명 등 고유명사만 필요시 영문을 병기한다.
- 투자 판단, 종목 비교, 매수·매도·비중조절 의견의 최우선 기준은 프로젝트 source 파일의 최신 보유종목·평가금액·보유비중이다. 반드시 해당 파일을 먼저 확인하고, 전체 포트폴리오 비중과 기존 중복 노출을 함께 반영한다. 기억에 의존한 과거 보유 비중이나 종목 목록은 사용하지 않는다.

### 수치 검증 원칙
- 차트·표·본문·툴팁의 모든 수치는 공시, 실적발표자료, 실적 컨퍼런스콜, 규제기관 제출자료 또는 신뢰 가능한 원문 보도로 검증 가능해야 한다.
- 계산값은 원자료, 기준일, 정의 및 산식을 함께 제시하거나 재현 가능해야 한다.
- 근거가 없거나 정의가 달라 비교할 수 없는 수치는 추정·임의 보정·직전 값 유지로 채우지 않고 `없음` 또는 `자료에서 확인되지 않음`으로 표시한다.
- 후속 확인에서 근거 없는 수치가 발견되면 즉시 삭제·정정한다.
- 사실과 해석을 명확히 구분하고, 불가피한 추정은 추정임을 표시하며 근거·가정·한계를 함께 설명한다.

### 분석 원칙
- 단기 주가 등락·목표가 변경보다 실적, 수주, CAPEX, 공급·수요, 기술 경쟁력, 시장점유율, 고객·계약, 전략, 규제, 현금흐름처럼 향후 기업가치와 EPS에 연결되는 근거를 우선한다.
- AI 인프라는 GPU·메모리·네트워크·광통신·전력·냉각·송배전·데이터센터 건설·클라우드 소프트웨어의 연결 구조로 분석한다. 빅테크 CAPEX 증가가 어느 밸류체인에, 어느 시차로, 어떤 실적 경로를 통해 반영되는지 설명한다.
- AI 상류 수요는 구조적 성장 가능성을 기본 가설로 두되, 주가 상승 판단은 밸류에이션 확대가 아니라 수주·매출·마진·EPS 개선으로 검증한다.
- 고점 추격을 피하고 투자 논리·실적 확인·가격 조건을 함께 충족하는 분할 접근을 우선한다. 실적이나 산업 논리가 약화되면 기존 수익률과 무관하게 재평가한다.
- 집중투자의 장점은 인정하되, 동일한 AI CAPEX 요인에 함께 민감한 종목과 ETF의 중복 노출을 반드시 점검한다. 새 매수 제안은 기존 보유 종목 대비 기대수익, 실적 가시성, 밸류에이션, 변동성, 중복도를 비교해 제시한다.
- **목표비중 초과를 기계적인 추가매수 금지로 해석하지 않는다.** 비중 초과는 추가매수 허들을 높이는 요인으로 사용하고, 향후 기대수익·EPS 리비전·사이클·중복 노출·하방 위험을 함께 비교해 조건부 추가매수를 허용할 수 있다.
- **L3 메모리 목표비중은 사이클에 따라 동적으로 운용한다.** 가속·초입은 38~45%(집중상한 50%), 성숙은 34~40%(집중상한 47%), 과열은 28~35%(집중상한 42%)를 기본 밴드로 사용한다. 집중상한은 정상 목표비중이 아니라 예외적 상단이며, 상한 접근 시 추가매수 허들을 크게 높인다.
- **L3 추가매수 판단은 MU를 대표 관측축으로 `γ 상태 + FY+1 EPS 30일/90일 리비전 + 30일 주가 상승률−EPS 리비전 차이`를 함께 본다.** 비중이 높더라도 EPS 개선 속도가 가격 상승보다 빠르고 γ가 열려 있으며 업황 근거가 강화되면 조건부 추가매수를 허용할 수 있다. 반대로 가격이 추정을 지속적으로 앞서거나 EPS 리비전이 둔화·하향되면 추가매수를 제한하고 트림 후보로 재평가한다.
- **매크로 매수 게이트는 위 집중·사이클 판단과 별도 규율로 유지한다.** 종목·레이어 매력도가 높더라도 매크로 게이트가 잠겨 있으면 신규매수 실행은 보류하며, 게이트가 열렸다고 해서 목표비중 상한이나 펀더멘털 검증을 생략하지 않는다.
- 현금 확보 또는 비중 축소가 필요할 때는 원칙적으로 수익이 난 자산부터 검토한다. 다만 투자 논리가 훼손됐거나 구조적 하락 위험이 커진 자산, 세금상 유리한 경우는 예외로 한다.
- 보유 종목 평가에서는 과거 수익률 자체를 판단 근거로 삼지 말고, 향후 3개월·6개월의 실적 개선 가능성, 촉매, 리스크, 포트폴리오 역할을 중심으로 판단한다.

### 종목 뉴스
- source 파일의 현재 보유 종목만 대상으로 하며, 목표가 변경·하루 단위 주가 등락·단순 수급 설명은 제외한다.
- 실적 및 가이던스, 수주·고객 계약·투자 및 CAPEX, 제품·기술·공급망 전략, 경쟁구도·가격·공급 변화, 규제·정책·거시 변수 중 실적 영향이 큰 내용만 남긴다.
- 종목별 뉴스와 투자 의미는 `1. 핵심 사실 2. 투자 의미 3. 긍정·부정 영향 4. 확인할 다음 지표 5. 현재 보유비중을 고려한 대응(확대·유지·축소·관찰 중 하나)` 순서로 간결하게 정리한다.

### 매수·매도·비중조절 의견
- 현재 보유비중과 동일 테마 중복 노출, 투자 논리와 핵심 촉매, 상승·반대 시나리오, 실적 확인 포인트와 무효화 조건, 우선순위(확대·유지·축소·관찰), 단일 가격 목표보다 분할 기준과 비중 범위를 반드시 함께 제시한다.
- 목표비중을 절대적인 매수·매도선으로 쓰지 말고, 사이클 단계와 기대수익에 따라 목표비중 범위 자체를 조정한다. 비중 초과 시에는 `매수 금지`보다 `추가매수 허들 상승`, 비중 미달 시에는 `기계적 채우기`보다 다른 보유자산 대비 기대수익 우위를 우선한다.
- 불확실한 사실, 최신 수치, 실적 일정, 정책 및 시장 데이터는 확인 가능한 최신 출처로 검증한다. 사실과 해석을 구분하고, 추정은 추정이라고 명확히 표시한다. 결론은 단정적으로 과장하지 말고, 현재 보유비중을 반영한 실행 가능한 판단으로 제시한다.

## index.html (~550KB 단일 파일)
- 절대 파일 전체를 재작성하지 않는다. 고유 문자열 앵커 기준 부분 치환만 사용.
- 핵심 앵커 const D= / const C= / HOLDINGS / SIGNAL_LOG / TARGETS 는 편집 후에도 보존되어야 한다.
- 편집 후 파일 크기가 비정상적으로 줄지 않았는지 확인.
- 대용량 패치는 `patches/*.b64` — **미니파이 + `base64 -w0` + 푸시 후 커밋SHA핀 raw 디코드 md5 왕복 검증**(OPS §6-3). 크기 일치는 무결성 보증이 아니다.

## JSON (alpha / earnings / judgment / gamma / holdings / signal_log / prices / news)
- 편집 시 유효한 JSON 을 유지한다. `signal_log.json`은 EOF 앵커 raw-text append(전체 재직렬화 금지).

## PR (중요: 자동 생성)
- 작업 후 "PR 생성 링크"만 제공하고 끝내지 말 것. 반드시 **직접 PR을 생성**한다.
- 변경을 작업 브랜치에 커밋·푸시한 뒤, gh pr create 로 PR을 직접 연다:
  `gh pr create --base main --head <작업브랜치> --fill`
  (gh 가 없거나 실패하면 사용 가능한 GitHub PR 생성 도구로 동일하게 PR을 만든다.)
- base 는 반드시 main. PR 제목/본문에 무엇을 왜 바꿨는지 한국어로 요약한다.
- PR을 실제로 생성한 뒤, 댓글에 생성된 PR 번호/링크를 보고한다. 링크 안내만으로 종료하지 않는다.

## 머지 결과 확인/보고 (중요)
- 이 리포는 `.github/workflows/claude-pr-gate.yml` 의 validate → auto-merge 잡이 base=main·head=claude/* PR 을 검사 통과 시 자동 squash 머지(+브랜치 삭제)한다.
- 작업 브랜치는 반드시 `claude/*` 형식을 사용해 자동 병합 대상에 포함한다.
- 따라서 PR 생성 후 "queued/진행 중" 상태로 끝내지 말 것. **게이트가 끝날 때까지 확인하여 실제 머지 여부를 명시 보고**한다.
- 보고에는 머지됨/안 됨을 분명히 적는다. 머지됐으면 squash 커밋 SHA(또는 (#PR번호) 커밋)와 head 브랜치 삭제 여부로 확정 짓는다.
- auto-merge 가 곧바로 안 떴으면 게이트 잡 상태를 재확인한다(생성 직후엔 queued 일 수 있음 — 그 스냅샷만 보고 "안 됐다"고 단정하지 말 것).
- 머지가 실패/대기로 끝났으면 그 사유(validate 실패 로그 등)를 보고한다.
- `.github/workflows/` 는 GitHub App 권한 부재로 403 → **운영자 수동 편집**.

## 문서 드리프트 가드
- UI/CSS 를 건드린 PR은 반드시 `node scripts/check-docs.mjs` 통과. 실패 시 `--fix` 후 이력 한 줄.
- `STYLE_GUIDE.md`의 `TOKENS:BEGIN~END`는 자동 생성 구역 — 손으로 고치지 않는다.
