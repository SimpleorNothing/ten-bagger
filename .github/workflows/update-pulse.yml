name: Update market pulse

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled by policy
        run: echo "Paid LLM market-pulse workflow disabled. ChatGPT scheduled routine owns this update."
