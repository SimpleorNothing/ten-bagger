name: Weekly Cycle Refresh (D·C·B·A)

# Paid LLM API execution is disabled. The ChatGPT scheduled routine performs the
# weekly research/judgment step and updates cycle.json through the normal PR flow.
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled by policy
        run: echo "Weekly Claude API refresh disabled; ChatGPT scheduled routine owns this update."
