name: Claude

# Paid LLM API execution is disabled by user policy (2026-08-13).
# Keep a manual no-op workflow so old links remain valid without spending API credits.
on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled by policy
        run: echo "Claude API workflow disabled."
